package main.java.model;

/**
 * Enum with the available metrics. These are LOC (Lines of Code), CYCLO
 * (McCabe's Cyclomatic Number), ATFD (Access To Foreign Data) and LAA (Locality
 * of Attribute Accesses).
 *
 */
public enum Metric {
	LOC, CYCLO, ATFD, LAA;
}
